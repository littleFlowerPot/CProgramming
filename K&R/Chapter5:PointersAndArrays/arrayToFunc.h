int strlen_mine(char s[]);
/* or ....(char *s), it does
 * not matter, and you can
 * mix the header declaration
 * and the function definition
 * freely (and pg 100 refers to mixing the two _within a
 * function definition_ when it makes sense to do so.
 */
